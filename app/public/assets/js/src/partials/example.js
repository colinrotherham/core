/*
	Example module
	------------------------------- */

	define(['jquery'], function($) {

		'use strict';

		// Test jQuery dependency
		if ($ && window.console)
			console.log('Hello, jQuery is ready');
	});
