/*
	App launcher
	------------------------------- */

	define('app', ['loadCSS', 'onloadCSS', 'loadJS'], function(loadCSS, onloadCSS, loadJS) {

		'use strict';

		// Other modules
		require(['example']);
	});
